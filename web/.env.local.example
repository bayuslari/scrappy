# Supabase — use the ANON (public) key here, NOT the service role key.
# Dashboard → Project Settings → API.
NEXT_PUBLIC_SUPABASE_URL=https://xxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
